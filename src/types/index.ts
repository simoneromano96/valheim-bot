import {
  ObserveMod,
  IObserveMod,
  ObservedMod,
  IObservedMod,
  ObservedModList,
  IObservedModList,
  ModInfo,
  IModInfo,
  ModInfoList,
  IModInfoList,
} from "./schemas"

// Enum of DB keys
export enum DBKeys {
  // The observed mod list
  OBSERVED_MOD_LIST = "OBSERVED_MOD_LIST",
  // The valuated mod list
  MOD_INFO_LIST = "MOD_LIST",
}

export {
  ObserveMod,
  IObserveMod,
  ObservedMod,
  IObservedMod,
  ObservedModList,
  IObservedModList,
  ModInfo,
  IModInfo,
  ModInfoList,
  IModInfoList,
}
